/* * */

import { StyleSheet } from 'react-native';

/* * */

export default StyleSheet.create({
	accentOption: {
		borderRadius: 999,
		borderWidth: 4,
		height: 35,
		width: 35,
	},
	container: {
		alignItems: 'center',
		display: 'flex',
		flexDirection: 'row',
		gap: 10,
		justifyContent: 'space-between',
		padding: 20,
		width: '100%',
	},
});
