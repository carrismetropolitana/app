/* * */

import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
	/* CONTAINER */
	container: {
		alignItems: 'center',
		backgroundColor: '#fff',
		flex: 1,
		justifyContent: 'center',
	},

	/* * */
	/* IMAGE */

	image: {
		height: 200,
		marginBottom: 20,
		width: 200,
	},

	/* * */
	/* SUBTITLE */

	subtitle: {
		color: '#666',
		fontSize: 16,
	},

	/* * */
	/* TITLE */

	title: {
		fontSize: 24,
		fontWeight: 'bold',
		marginBottom: 10,
	},
});
