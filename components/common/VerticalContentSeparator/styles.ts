/* * */
import { StyleSheet } from 'react-native';
/* * */

/* * */
const styles = () => {
	return StyleSheet.create({

		svg: {
			marginBottom: 20,
			marginTop: 20,
		},
	});
};
export default styles;
