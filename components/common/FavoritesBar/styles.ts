/* * */
import { theming } from '@/theme/Variables';
import { StyleSheet } from 'react-native';

/* * */

export const styles = () => {
	return StyleSheet.create({
		/* CONTAINER */
		container: {
			height: 30,
			marginBottom: 20,
			width: '100%',
		},

		/* * */
		/* ITEM */

		item: {
			paddingLeft: 5,
		},
	});
};
