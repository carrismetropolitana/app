/* * */
import { theming } from '@/theme/Variables';
import { StyleSheet } from 'react-native';

/* * */

export const styles = () => {
	return StyleSheet.create({
		/* CONTAINER */
		container: {
			height: 50,
			marginBottom: 20,
			width: '100%',
		},

		/* * */
		/* ITEM */

		item: {
			paddingTop: 10,
			paddingLeft: 12,
		},
	});
};
