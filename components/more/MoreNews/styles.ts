/* * */

import { StyleSheet } from 'react-native';

/* * */

export default StyleSheet.create({
	container: {
		display: 'flex',
		flexDirection: 'row',
		gap: 20,
		overflowX: 'scroll',
		padding: 20,
	},
});
